# Windows Client

Minimal Windows demo shell that calls into the Core library.

## Run

```bash
dotnet run --project src/WindowsClient/WindowsClient.csproj
```

## Demo commands
- `connect`
- `disconnect`
- `fail`
- `status`
- `exit`
